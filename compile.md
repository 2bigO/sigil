# Retired temporary compile plan

This planning document is retained only as a repository pointer. The native workflow is self-contained: use the installed `sigil` skill and `sigilc --help`. Durable requests, captures, and completion evidence are in `.sigil/workflow/`; disposable worker artifacts are in `.sigil/tmp/`.
