interface:
  display_name: "Sigil"
  short_description: "Author Sigil and use native scope and semantic gates"
  default_prompt: "Use $sigil to inspect governing contracts, export current Design input, read the durable compilation execution reference, use sigilc directly for ordered scope, and when reconstruction is required spawn a subagent in the background for each source using that reference's exact isolated prompt; have it write the version-2 evidence manifest with a final pending exit null and run sigilc ingest --evidence, repairing its temporary Turtle from each hint until an exit-0 projection is published before native semantic gates. Never depend on temporary compile.md or track.md instructions."
