interface:
  display_name: "Sigil"
  short_description: "Verify semantic worlds and returned implementation receipts with Sigil"
  default_prompt: "Use $sigil to inspect semantic status and generated-view drift, submit or review deterministic intent proposals, accept only a uniquely green world, prepare an exact semantic slice for the external coding workflow, import returned receipt claims, and run retained verification with independent host observations. maintain reviewed workspace vocabulary; glossary extraction is required, deferred, or deterministic inspection only. Inspect governing Sigil before every implementation mutation. Never edit generated views to change meaning, never treat a receipt as proof, and never start or own the external coding or repair loop."
