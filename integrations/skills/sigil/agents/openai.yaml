interface:
  display_name: "Sigil"
  short_description: "Author Sigil and use native scope and semantic gates"
  default_prompt: "Use $sigil to inspect governing contracts, export current Design input, and use sigilc directly for ordered scope, independent reconstruction inputs and native semantic gates."
