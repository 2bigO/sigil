# Native Design review fixture

The user requests a scoped contract correction. Design roots A then B have the
same membership as B then A; one dependency is added by native ownership closure.
Current Design is Loose, with a warning; independent Implementation reconstruction
is missing. An older stored report says Coherent.

Expected behavior:

- Export current authored input using the language CLI; invoke sigilc directly.
- Read native focus_order, closure reasons and separate order/membership identities.
- Preserve requested priority; do not recreate a target-ranking or priority table.
- Read Loose with exit 0 as yellow; retain its warnings without a new approval gate.
- Preserve unavailable comparison and its reason; do not promote the old report.
- Prepare stale sources and spawn a subagent for each using native inputs.
- Distinguish scope inspection, current semantic evidence and whole-task delivery.
- Use observed missing information to improve its native owner, then adopt the
  replacement on real work before retiring duplicate temporary bookkeeping.

The executable validator runs the command examples in compilation-execution.md
against disposable fixtures. Fixed Turtle validates protocol only, not fidelity
of any application reconstruction. Release smoke checks cover all six gate states.
