# Architecture and design

Inspect the selected component and relevant workspace evidence directly.
Evaluate component boundaries, dependency direction, cohesion, coupling, and the
consistency of public contracts with private operational rationale. Use targeted
`sigil graph` and `sigil context` queries when relationships matter. Findings
must cite exact workspace evidence. Do not edit files, use the network, or run
implementation experiments.
