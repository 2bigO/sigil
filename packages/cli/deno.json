{
  "name": "@qoherent/sigil",
  "version": "0.8.0",
  "exports": "./src/main.ts",
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  },
  "publish": {
    "include": [
      "README.md",
      "LICENSE",
      "architecture.md",
      "spec.md",
      "src/**/*.ts"
    ],
    "exclude": [
      "tests/"
    ]
  },
  "tasks": {
    "install": "deno install --global --force --config ../../deno.json --allow-read --allow-write --allow-run --allow-env --name sigil src/main.ts",
    "test": "deno test --allow-env --allow-read --allow-write --allow-run",
    "publish:dry-run": "deno publish --dry-run"
  },
  "compilerOptions": {
    "strict": true
  }
}
