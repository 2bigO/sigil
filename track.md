# Retired temporary loop tracker

This tracker no longer defines the implementation loop. Use the installed `sigil` skill and `sigilc --help`; native request state, accepted projections, and completion evidence live under `.sigil/workflow/` and `.sigil/worlds/`. Put disposable worker artifacts under `.sigil/tmp/`.
