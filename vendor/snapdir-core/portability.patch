--- upstream/Cargo.toml
+++ vendored/Cargo.toml
@@ -3,7 +3,7 @@
-version.workspace = true
-edition.workspace = true
-rust-version.workspace = true
-authors.workspace = true
-license.workspace = true
-repository.workspace = true
-homepage.workspace = true
+version = "1.10.0"
+edition = "2021"
+rust-version = "1.91.1"
+authors = ["Bermi Ferrer <bermi@bermilabs.com>"]
+license = "MIT"
+repository = "https://github.com/bermi/snapdir"
+homepage = "https://snapdir.org"
@@ -20 +20 @@
-thiserror.workspace = true
+thiserror = "2"
@@ -24,3 +23,0 @@
-
-[lints]
-workspace = true
--- upstream/src/walk.rs
+++ vendored/src/walk.rs
@@ -45,0 +46 @@
+#[cfg(unix)]
@@ -285,0 +287 @@
+#[cfg(unix)]
@@ -287,0 +290,8 @@
+}
+
+// Windows permissions are documentary only, never Sigil semantic identity.
+fn portable_permissions(metadata: &std::fs::Metadata) -> String {
+    #[cfg(unix)]
+    { octal_permissions(metadata.permissions().mode()) }
+    #[cfg(not(unix))]
+    { if metadata.permissions().readonly() { "444" } else { "666" }.to_string() }
@@ -478 +488 @@
-    let root_permissions = octal_permissions(root_lstat.permissions().mode());
+    let root_permissions = portable_permissions(&root_lstat);
@@ -685 +695 @@
-        let own_permissions = octal_permissions(link_meta.permissions().mode());
+        let own_permissions = portable_permissions(&link_meta);
@@ -927,0 +938 @@
+            #[cfg(unix)]
--- upstream/src/resources.rs
+++ vendored/src/resources.rs
@@ -37,0 +38 @@
+#[cfg(unix)]
@@ -49,0 +51,3 @@
+
+#[cfg(not(unix))]
+fn rusage_cpu_secs() -> Option<f64> { None }
